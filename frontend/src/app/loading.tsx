import { LoadingScreen } from '@/components/shared/loading-screen';

export default function AppLoading() {
  return (
    <LoadingScreen
      title="Preparing BuzzMap"
      message="We are loading the next page and getting everything ready."
    />
  );
}
